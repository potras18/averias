org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Use Google Play Services' shared MLKit barcode module instead of the one
# bundled into the APK — the bundled native binding was crashing in release
# builds with a NullPointerException inside an already-minified vendor class.
dev.steenbakker.mobile_scanner.useUnbundled=true
